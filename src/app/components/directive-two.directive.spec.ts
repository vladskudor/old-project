import { DirectiveTwoDirective } from './directive-two.directive';

describe('DirectiveTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveTwoDirective();
    expect(directive).toBeTruthy();
  });
});
